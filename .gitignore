# Environment files
.env
.env.local
.env.*.local

# Dependencies
node_modules/
vendor/

# Build output
public/build/
public/hot

# Laravel
storage/
bootstrap/cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db
.DS_Store

# Temp
*.tmp
*.log
.env.runtime

# Dumps de la base de producción (datos de clientes) — nunca al repo público
backups/

# El compose real SÍ se versiona: las credenciales salen de .env (ver .env.example).
docker-compose.yml.bak-*

# Lleva la server_key del builder, que debe coincidir con la fila de la tabla
# `builders` en la DB. Es distinta por instalación; la plantilla es config.example.json
Builder/prebuilt/config.json
