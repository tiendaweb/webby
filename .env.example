# Lo lee `docker compose` para interpolar docker-compose.yml.
# NO es el .env de Laravel — ese es .env.runtime, montado dentro del contenedor.
# Copiar a .env y poner las credenciales reales; .env está en .gitignore
# porque este repo es público.
DB_PASSWORD=cambiar-esto
DB_ROOT_PASSWORD=cambiar-esto-tambien
